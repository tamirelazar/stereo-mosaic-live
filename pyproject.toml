[project]
name = "smlive"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy>=2", "scipy>=1.13", "scikit-image", "imageio", "matplotlib"]

[project.scripts]
smlive = "smlive.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
